name: ci
on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # One entry point, so CI and a local run cannot drift apart.
      # Everything here works without a board attached.
      - run: ./tests/run-all.sh
